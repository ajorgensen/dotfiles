#!/usr/bin/env bash

if test ! $(which gpg); then
  if test ! $(which brew); then
    source "homebrew.install"
  fi

  brew install gpg pinentry
else
  echo "GPG/Pinentry already installed"
fi

exit 0
